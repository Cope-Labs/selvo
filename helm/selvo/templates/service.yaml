apiVersion: v1
kind: Service
metadata:
  name: {{ include "selvo.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "selvo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "selvo.selectorLabels" . | nindent 4 }}
